<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="240dp"
    android:layout_margin="6dp">

    <!-- Photo Image with rounded corners -->
    <ImageView
        android:id="@+id/iv_photo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/app_name"
        android:background="@drawable/bg_photo_rounded" />

    <!-- Profile Badge -->
    <TextView
        android:id="@+id/tv_profile_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="8dp"
        android:background="@drawable/bg_badge"
        android:paddingHorizontal="8dp"
        android:paddingVertical="4dp"
        android:text="⭐ Profile"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:visibility="gone" />

    <!-- Subtle overlay on hover - light gradient -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_overlay_light"
        android:visibility="gone" />

    <!-- Button Container - Top Right & Bottom Right -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="bottom|end"
        android:padding="8dp">

        <!-- Set as Profile Button - Top Right -->
        <ImageButton
            android:id="@+id/btn_set_as_profile"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_margin="4dp"
            android:background="@drawable/bg_button_circular"
            android:contentDescription="Set as Profile"
            android:scaleType="centerInside"
            android:src="@drawable/ic_star"
            android:visibility="gone" />

        <!-- Delete Button - Bottom Right -->
        <ImageButton
            android:id="@+id/btn_delete_photo"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_margin="4dp"
            android:background="@drawable/bg_button_circular_danger"
            android:contentDescription="Delete Photo"
            android:scaleType="centerInside"
            android:src="@android:drawable/ic_menu_delete"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
